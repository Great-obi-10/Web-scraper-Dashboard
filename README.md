# Web Scraper Dashboard

Beautiful live dashboard (exactly like the screenshot) + FastAPI backend + scraper.

## Run it
```bash
pip install -r requirements.txt
uvicorn backend.app:app --reload
# Open frontend/index.html